node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
server/storage/
server/.env

# Browser QA session captures (not source)
.playwright-mcp/

# Local dev credentials (never commit)
DEV_CREDENTIALS.md

# Local Claude Code config + worktrees (not source)
.claude/

# worktree scratch / symlinked deps (SDD)
/node_modules
/server/node_modules
.superpowers/
